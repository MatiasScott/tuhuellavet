document.addEventListener('DOMContentLoaded',()=>{
 const d=window.TreatmentData||{};const box=document.getElementById('treatment-medications');
 function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
 function addRow(){if(!box)return;const i=box.children.length;const div=document.createElement('div');div.className='soft-panel form-grid mb-1';div.innerHTML=`<label><span>Fármaco</span><select name="medicamentos[${i}][farmaco_id]"><option value="">Seleccionar</option>${(d.drugs||[]).map(x=>`<option value="${x.id}">${esc(x.nombre)}</option>`).join('')}</select></label><label><span>Dosis</span><input type="number" step="any" name="medicamentos[${i}][dosis_cantidad]"></label><label><span>Unidad dosis</span><select name="medicamentos[${i}][dosis_unidad_id]"><option value="">—</option>${(d.units||[]).map(x=>`<option value="${x.id}">${esc(x.simbolo||x.nombre)}</option>`).join('')}</select></label><label><span>Vía</span><select name="medicamentos[${i}][via_administracion_id]"><option value="">—</option>${(d.routes||[]).map(x=>`<option value="${x.id}">${esc(x.nombre)}</option>`).join('')}</select></label><label><span>Frecuencia</span><select name="medicamentos[${i}][frecuencia_id]"><option value="">—</option>${(d.frequencies||[]).map(x=>`<option value="${x.id}">${esc(x.nombre)}</option>`).join('')}</select></label><label><span>Duración</span><input type="number" step="any" name="medicamentos[${i}][duracion_cantidad]"></label><label><span>Unidad tiempo</span><select name="medicamentos[${i}][duracion_unidad_id]"><option value="">—</option>${(d.timeUnits||[]).map(x=>`<option value="${x.id}">${esc(x.nombre)}</option>`).join('')}</select></label><label class="field-full"><span>Instrucciones</span><input name="medicamentos[${i}][instrucciones]"></label>`;box.appendChild(div);}
 document.querySelectorAll('[data-add-medication]').forEach(b=>b.addEventListener('click',addRow));if(box&&box.children.length===0)addRow();
 document.querySelectorAll('[data-medication-apply]').forEach(b=>b.addEventListener('click',()=>{const form=document.getElementById('medication-apply-form');if(!form)return;form.action=(d.applicationUrlBase?d.applicationUrlBase+'/'+b.dataset.medicationId+'/aplicar':(d.baseUrl||'')+'/consultas/'+d.eventId+'/tratamientos/medicamentos/'+b.dataset.medicationId+'/aplicar');document.getElementById('medication-apply')?.classList.add('is-open');}));
});
