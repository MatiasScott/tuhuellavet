(function () {
    const readyMessage = 'Modulo de animales listo para integracion de filtros por empresa y especie.';
    if (window.console && typeof window.console.info === 'function') {
        window.console.info(readyMessage);
    }
})();
