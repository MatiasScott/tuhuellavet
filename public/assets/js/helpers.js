document.addEventListener('click',e=>{const open=e.target.closest('[data-modal-open]');if(open){const m=document.getElementById(open.dataset.modalOpen);if(m){m.classList.add('is-open');document.body.classList.add('modal-open');}}const close=e.target.closest('[data-modal-close]');if(close){const m=close.closest('.modal');m?.classList.remove('is-open');document.body.classList.remove('modal-open');}if(e.target.classList.contains('modal-backdrop')){e.target.closest('.modal')?.classList.remove('is-open');document.body.classList.remove('modal-open');}});document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelectorAll('.modal.is-open').forEach(m=>m.classList.remove('is-open'));});
